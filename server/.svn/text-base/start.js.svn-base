
var server = require('./server');
var router = require('./router');
//var n_cpu = require('os').cpus().length;

server.start(router.route);

var cp = require('child_process');

process.send_order = cp.fork('../app/tasks/order');

console.log('create send order process ' + process.send_order.pid);

/*
process.transfer_trade= [];
process.transfer_trade.push(cp.fork('../app/tasks/transfer'));
process.transfer_trade.push(cp.fork('../app/tasks/transfer'));
process.transfer_trade.push(cp.fork('../app/tasks/transfer'));
process.transfer_trade.push(cp.fork('../app/tasks/transfer'));

process.transfer_trade.forEach(function(ps) {
    console.log('create work process ' + ps.pid); 
});
*/

